spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.kafka.autoconfigure.KafkaAutoConfiguration
      - org.springframework.boot.data.redis.autoconfigure.DataRedisAutoConfiguration
      - org.springframework.boot.data.redis.autoconfigure.DataRedisRepositoriesAutoConfiguration
  task:
    scheduling:
      # Tests drive expiration/outbox synchronously; no background races.
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/flashreserve_test
    username: flashreserve
    password: flashreserve_local_dev
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    clean-disabled: false

flashreserve:
  kafka:
    enabled: false
